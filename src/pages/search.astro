---
import Base from '../layouts/Base.astro';
import Search from '../components/Search.astro';
---

<Base title="Search">
  <div class="search-wrapper" style="animation: fadeSlideIn 0.35s ease both">
    <nav aria-label="Breadcrumb" class="breadcrumb">
      <a href="/">Home</a>
      <span class="breadcrumb-sep" aria-hidden="true">›</span>
      <span class="breadcrumb-current" aria-current="page">Search</span>
    </nav>

    <h1 class="section-title" style="margin-bottom: 0.5rem">Search Notes</h1>
    <p class="section-subtitle" style="margin-bottom: 2rem; text-transform: none; font-size: 0.9rem; letter-spacing: 0; color: var(--muted)">
      Full-text search across all 1,135 notes
    </p>

    <Search id="search" uiOptions={{ showSubResults: true, resetStyles: false }} />

    <style>
      :root {
        --pagefind-ui-font: 'Newsreader', serif;
        --pagefind-ui-primary: var(--accent);
        --pagefind-ui-text: var(--ink);
        --pagefind-ui-background: var(--surface);
        --pagefind-ui-border: var(--border);
        --pagefind-ui-border-radius: 6px;
        --pagefind-ui-scale: 1;
      }
    </style>
  </div>
</Base>
